# Filesystem MCP Server

1. Official [docs](https://github.com/modelcontextprotocol/servers/tree/main/src/filesystem)

2. Setup in Visual Studio Code (npx):
```json
"mcp": {
  "servers": {
    "filesystem": {
      "command": "npx",
      "args": [
        "-y",
        "@modelcontextprotocol/server-filesystem",
        "D:\\Temp"
      ]
    }
  }
}
```
